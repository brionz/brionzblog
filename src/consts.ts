/**
 * @deprecated Prefer importing from `src/config/brand.ts`.
 * Kept so existing imports (`SITE_TITLE`, `SITE_DESCRIPTION`) keep working.
 */
export { brand, SITE_DESCRIPTION, SITE_TITLE } from './config/brand';
